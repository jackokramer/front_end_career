const blue = '000f';
const orange = '#f60';

//const colors = `${blueColor} ${orangeColor}`

// Objects -datastcutres allow us for powerful patterns for managaing and relating data. Objects are like file cabinenets

{
    key: 'value' // every key is just a simple javascript strng while the value can be booleans or any data type even an object
}

//example

const obj = {
    sayHi() {
        console.log('hi')
    }
}

obj.sayHi()


// alert(), prompt() methods for objects, objectName();

const colors = {
    yellow: '#ff0',
    blue,
    orange
}
console.log(colors);